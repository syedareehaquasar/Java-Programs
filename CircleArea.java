public class CircleArea {

    public static void main(String[] args) {

        double radius = Double.valueOf(args[0]);

        System.out.println("Area of circle with radius " + radius + " is " + (radius * radius * 3.14));
    }
}
